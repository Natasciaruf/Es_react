import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';  // Importiamo il componente principale

ReactDOM.render(
  <React.StrictMode>
    <App />  {/* Renderizziamo il componente App */}
  </React.StrictMode>,
  document.getElementById('root')
);
